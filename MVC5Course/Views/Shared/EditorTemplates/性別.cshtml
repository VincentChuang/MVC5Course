@model String
@if (Model != null) {
    <label>@Html.RadioButton("", "M", Model.Trim() == "M") 男性</label>
    <label>@Html.RadioButton("", "F", Model.Trim() == "F") 女性</label>
} else {
    <label>@Html.RadioButton("", "M") 男性</label>
    <label>@Html.RadioButton("", "F") 女性</label>
}